# GFS Landing Page

Landing page Next.js per GFS Solutions.

## Avvio locale
```bash
npm install
npm run dev
```
Apri `http://localhost:3000`.

## Note
Le variabili d'ambiente sono ignorate da Git (`.env*`).
